generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Record {
  id    Int    @id @default(autoincrement())
  title String
  time  Int
}
